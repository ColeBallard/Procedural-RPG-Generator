[pytest]
markers =
    llm: tests that issue real calls to the Grok LLM API (auto-skipped when GROK_API_KEY is unset)
    slow: long-running tests (e.g., full end-to-end world builds)
testpaths = tests
